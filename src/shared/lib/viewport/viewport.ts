/** Shared viewport breakpoints (single source of truth for layout/media logic). */

export const DESKTOP_MIN_WIDTH_PX = 1024;

export const MEDIA_QUERY_DESKTOP = `(min-width: ${DESKTOP_MIN_WIDTH_PX}px)`;
